import { Box, Typography } from '@mui/material';
import React from 'react';

const Footer = () => {
  return (
    <Box>
      <Typography> Footer test</Typography>
    </Box>
  );
};

export default Footer;
